# tuples are similar to lits but have one difference that list is changeable but tuple is not

# Single values needs comma to become tuple
fruits = ('Apple','Mango','Orange')

print(fruits, type(fruits))

# Don't allow this
fruits[1] = 'Bnana'

print(fruits)